// https://leetcode.com/problems/merge-k-sorted-lists/

class ListNode {
    constructor(val, next) {
        this.val = val === undefined ? 0 : val
        this.next = next === undefined ? null : next
    }
}

const mergeKLists = (ransomNote, magazine) => {

};

const run = () => {
    const result = mergeKLists('a', 'b')

    console.log(result)
}

export default run

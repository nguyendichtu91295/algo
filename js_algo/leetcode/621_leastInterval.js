// https://leetcode.com/problems/task-scheduler/

const leastInterval = (tasks, n) => {
    // const counter = {}
    // tasks.forEach(each => {
    //     counter[each] = counter[each] ? counter[each] + 1 : 1
    // });

    // const minHeap = new Heap()
    // for (const key in counter) {
    //     if (Object.hasOwnProperty.call(counter, key)) {
    //         const element = counter[key];

    //         minHeap.insert([])
    //     }
    // }

    // let time = 0;
    // while() {
    //     // break
    //     time += 1
    // }

    // return time
}

const run = () => {
    const result = leastInterval(['A', 'A', 'A', 'B', 'B', 'C', 'C'], 2)

    console.log(result)
}

export default run
